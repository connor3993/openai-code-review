# 2026-07-10 工作日志

## 上午/下午：openai-code-review-moni 项目搭建与 Spring AI 方案讨论

### 完成
- 在 `F:\JavaWebProjects\openai-code-review-moni`（新项目，com.connor）搭建 Maven 多模块骨架
- 父 POM 引入 spring-boot-starter-parent 3.3.5，`<java.version>21</java.version>`
- SDK 模块：`lombok` + `jgit 6.10.0` + `fastjson2 2.0.49` + `java-jwt 4.2.2`
- test 模块：`spring-boot-starter-web` + `spring-boot-starter-test`（自带 JUnit 5）+ `lombok` + 引入本地 SDK
- `Application.java` 改为标准 `@SpringBootApplication` 启动类
- `ApiTest.java` 改为 JUnit 5 写法（`org.junit.jupiter.api.Test` + `@SpringBootTest`，**已删除 JUnit 4 的 `@RunWith` 注解**）
- `mvn clean compile` 通过，`mvn test` 通过，看到 `Spring Boot :: (v3.3.5)` + `测试 Spring Boot 3 + JUnit 5` 日志

### 关键发现
- 用户的 Maven 环境 `JAVA_HOME=F:\Javajdks\jdk21` 配置在 F 盘，但用户以为在 E 盘（`/e/Javajdks/jdk21`），造成环境混淆
- 用户工作环境用的是阿里云镜像（`maven.aliyun.com/repository/public`），依赖下载正常
- Spring AI 版本选择：用户问过 2.0，最终建议**用 1.x + SB 3.3.5**（更稳），但留待用户决定

### 下一步（用户确认后）
- 把原项目 `OpenAiCodeReview`、`GitCommand`、`ChatGLM`、`WeiXin` 等核心类迁移到 moni 项目
- 升级到 Spring Boot 3.3.5 + 改造 `ChatGLM` 为 `ChatClient`
- 结构化输出 `CodeReviewResult`（评分/优点/问题/建议/改后代码）
- 升级 JGit 到 6.10.0
